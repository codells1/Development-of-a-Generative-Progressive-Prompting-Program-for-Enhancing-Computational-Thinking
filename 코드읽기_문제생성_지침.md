# 코드 읽기 문제 생성 지침 (Code-Reading Item Generation Guide)

> LLM(예: Qwen3 8B)에게 **"하나의 완결된 파이썬 코드 + 5개 사고력 문제"** 를 한 번에 생성하도록 지시하기 위한 사양서입니다.

---

## 0. 무엇을 만들려는가

기존의 "단계별 코드 조각" 방식에서 벗어나, **그 자체로 실행 가능한 하나의 작은 프로그램**을 학생에게 제시하고, 그 코드를 읽고 답하는 5개 유형의 이해 문제(분해 · 패턴인식 · 추상화 · 알고리즘 · 통합)를 생성한다.

- **입력**: 난이도, 주제(선택), 코드 길이 등 파라미터 (→ 6장)
- **출력**: JSON 1건 = `코드 1개` + `5문항` (→ 4장)

---

## 1. 핵심 원칙 (가장 중요)

1. **코드는 조각이 아니라 완결된 프로그램이다.** 위에서 아래로 끝까지 실행되어 의미 있는 결과를 내야 한다.
2. **기능은 단순해야 한다.** 학생이 "이 프로그램이 무엇을 하는지"를 한 문장으로 떠올릴 수 있어야 한다.
3. **문제는 코드를 *읽어서* 답하는 것이다.** 실행 결과를 외우는 게 아니라, 구조와 흐름을 사고하게 한다.
4. **5개 사고력이 모두 성립하도록 코드를 설계한다.** 문제를 먼저 떠올리지 말고, 코드 안에 5개 유형의 "재료"가 다 들어 있는지 확인한다(→ 2.2).

---

## 2. 코드 생성 지침

### 2.1 코드의 조건

| 항목 | 기준 |
|---|---|
| 형태 | 실행 가능한 단일 프로그램 (조각 X) |
| 길이 | **약 30~50줄** (함수 3~5개 권장) |
| 함수 | 2개 이상으로 기능을 나눌 것 |
| 의존성 | 표준 라이브러리만, 가급적 `import` 없이. 외부 입력 없이 실행되도록 데이터는 코드 안에 고정 |
| 이름 | 변수·함수 이름은 **의미가 드러나게** (코드 읽기 학습이므로 `a`, `x` 금지) |
| 주석 | **기본은 최소화 또는 제거** — 학생이 스스로 해석하게. (파라미터로 켤 수 있음) |
| 결과 | `print`로 사람이 읽을 수 있는 결과를 출력 |

> **길이에 대한 주의**: "길게"라고 해서 60줄을 넘기면 중·고생에게는 인지 부하가 급격히 커진다. 5개 문제를 풍부하게 만들 수 있는 **최소한의 충분한 길이(30~50줄)** 가 목표다. 길이는 줄 수가 아니라 "구성요소가 다 들어갔는가"로 판단한다.

### 2.2 5개 사고력을 담기 위한 필수 구성요소 ✅

코드를 생성한 뒤, 아래가 **모두** 들어 있는지 자체 점검한다. 하나라도 없으면 코드를 보강한다.

- [ ] **분해용** — 뚜렷이 구분되는 함수/기능 블록이 2개 이상 있는가
- [ ] **패턴인식용** — 반복문 또는 유사한 구조의 반복이 있는가 (누적 합 패턴, `if-elif` 사다리, 비슷한 두 함수 등)
- [ ] **추상화용** — 세부 동작을 함수·매개변수 뒤로 숨긴 부분이 있는가 (이름만 봐도 역할을 짐작할 수 있는가)
- [ ] **알고리즘용** — 조건 분기나 순차적 처리 절차가 코드에 드러나 있는가
- [ ] **통합용** — 작은 부분들이 모여 하나의 최종 목적을 이루는 구조가 있는가 (예: 메인 반복문이 여러 함수를 호출해 전체를 완성)

---

## 3. 문제 생성 지침

### 3.1 모든 문항 공통 규칙

- **코드를 읽으면 답할 수 있어야** 한다. (정답 암기형 X)
- 가능하면 **구체적인 함수명·줄·변수**를 가리켜 질문한다.
- 발문은 단답형이 아니라 **"설명해 보세요 / 비교해 보세요 / 추론해 보세요"** 형태로 사고를 유도한다.
- 대상 학년 수준의 어휘를 쓴다.
- 각 유형당 **1문항**을 기본으로 한다(파라미터로 조정 가능).

### 3.2 유형별 정의 · 질문 초점 · 발문 템플릿

#### ① 분해 (Decomposition)
- **정의**: 큰 문제를 다루기 쉬운 작은 단위로 나누는 사고.
- **질문 초점**: 이 프로그램이 어떤 하위 기능들로 나뉘어 있는가, 각 부분의 역할은 무엇인가.
- **발문 템플릿**:
  - "이 프로그램은 여러 함수로 나뉘어 있습니다. 각 함수가 맡은 일을 하나씩 설명하고, 전체 문제가 어떤 작은 문제들로 나뉘었는지 정리해 보세요."

#### ② 패턴인식 (Pattern Recognition)
- **정의**: 반복되거나 규칙적으로 나타나는 구조를 알아채는 사고.
- **질문 초점**: 반복되는 코드 구조, 유사한 함수, 규칙적인 조건 분기.
- **발문 템플릿**:
  - "코드에서 반복되거나 서로 비슷하게 생긴 구조를 찾아보세요. 두 부분이 왜 비슷한지, 어떤 공통 규칙을 따르는지 설명해 보세요."

#### ③ 추상화 (Abstraction)
- **정의**: 불필요한 세부를 감추고 핵심만 다루는 사고.
- **질문 초점**: 함수/변수가 무엇을 대표하는가, 함수가 어떤 세부 동작을 숨기고 있는가.
- **발문 템플릿**:
  - "○○ 함수는 내부의 복잡한 과정을 숨기고 결과만 돌려줍니다. 이 함수가 무엇을 '대표'하는지, 이름만 보고도 역할을 짐작할 수 있는 이유는 무엇인지 설명해 보세요."

#### ④ 알고리즘 (Algorithm)
- **정의**: 문제를 푸는 단계와 순서, 즉 절차적 흐름을 이해하는 사고.
- **질문 초점**: 코드가 실행되는 순서, 조건에 따른 흐름 분기.
- **발문 템플릿**:
  - "○○ 함수에 △△ 값이 들어왔다고 가정해 봅시다. 코드가 어떤 조건들을 어떤 순서로 검사하며, 최종적으로 무엇을 돌려주는지 차례대로 설명해 보세요."

#### ⑤ 통합 (Integration)
- **정의**: 나뉜 부분들을 다시 합쳐 전체가 어떻게 동작하는지 보는 사고.
- **질문 초점**: 부분들이 어떤 순서로 협력하는가, 전체 프로그램의 최종 목적은 무엇인가.
- **발문 템플릿**:
  - "여러 함수가 어떤 순서로 함께 사용되나요? 그리고 메인 반복문은 이 부분들을 어떻게 하나로 합쳐 프로그램 전체의 목적을 완성하나요?"

---

## 4. 출력 형식 (JSON)

LLM은 **오직 아래 JSON만** 출력한다. 코드블록 표시(```)나 설명 문장을 앞뒤에 붙이지 않는다.

```json
{
  "title": "프로그램 제목",
  "summary": "이 프로그램이 하는 일 한 줄 설명",
  "difficulty": "중3 ~ 고1",
  "code": "실행 가능한 파이썬 코드 전체 (줄바꿈은 \\n)",
  "questions": [
    {
      "category": "decomposition",
      "category_ko": "분해",
      "question": "발문",
      "focus_points": ["채점/안내용 핵심 포인트 1", "핵심 포인트 2"]
    },
    {
      "category": "pattern_recognition",
      "category_ko": "패턴인식",
      "question": "발문",
      "focus_points": ["...", "..."]
    },
    {
      "category": "abstraction",
      "category_ko": "추상화",
      "question": "발문",
      "focus_points": ["...", "..."]
    },
    {
      "category": "algorithm",
      "category_ko": "알고리즘",
      "question": "발문",
      "focus_points": ["...", "..."]
    },
    {
      "category": "integration",
      "category_ko": "통합",
      "question": "발문",
      "focus_points": ["...", "..."]
    }
  ]
}
```

> **`focus_points`의 용도**: 학생에게 그대로 보여주는 정답이 아니라, **소크라테스식 챗봇이 답을 직접 알려주지 않고 유도할 때의 내부 기준**이자, 세션 후 CT 평가의 채점 근거로 쓰는 항목이다. UI의 학생 화면에는 노출하지 않는다.

---

## 5. 완성 예시 (Few-shot)

아래는 위 지침을 모두 충족하는 예시다. 프롬프트에 이 예시를 함께 넣으면 출력 품질이 안정된다.

```json
{
  "title": "학생 성적 처리 프로그램",
  "summary": "여러 학생의 시험 점수로 평균·최고점·등급을 계산해 성적표를 출력한다.",
  "difficulty": "중3 ~ 고1",
  "code": "def calculate_average(scores):\n    total = 0\n    for score in scores:\n        total += score\n    return total / len(scores)\n\ndef get_grade(average):\n    if average >= 90:\n        return \"A\"\n    elif average >= 80:\n        return \"B\"\n    elif average >= 70:\n        return \"C\"\n    elif average >= 60:\n        return \"D\"\n    else:\n        return \"F\"\n\ndef find_highest(scores):\n    highest = scores[0]\n    for score in scores:\n        if score > highest:\n            highest = score\n    return highest\n\ndef print_report(name, scores):\n    average = calculate_average(scores)\n    grade = get_grade(average)\n    highest = find_highest(scores)\n    print(f\"학생 이름: {name}\")\n    print(f\"점수: {scores}\")\n    print(f\"평균: {average:.1f}\")\n    print(f\"최고점: {highest}\")\n    print(f\"등급: {grade}\")\n    print(\"-\" * 20)\n\nstudents = {\n    \"민수\": [85, 92, 78],\n    \"지영\": [95, 88, 91],\n    \"현우\": [55, 60, 48]\n}\n\nfor name, scores in students.items():\n    print_report(name, scores)",
  "questions": [
    {
      "category": "decomposition",
      "category_ko": "분해",
      "question": "이 프로그램은 calculate_average, get_grade, find_highest, print_report 네 개의 함수로 나뉘어 있습니다. 각 함수가 맡은 역할을 하나씩 설명하고, '성적 처리'라는 큰 문제가 어떤 작은 문제들로 나뉘었는지 정리해 보세요.",
      "focus_points": [
        "calculate_average=평균 계산, get_grade=등급 판정, find_highest=최고점 찾기, print_report=결과 출력으로 역할 구분",
        "큰 문제(성적표 만들기)가 '계산 → 판정 → 출력'의 하위 문제로 분해되었음을 인식"
      ]
    },
    {
      "category": "pattern_recognition",
      "category_ko": "패턴인식",
      "question": "calculate_average 함수와 find_highest 함수를 비교해 보세요. 두 함수에서 반복적으로 나타나는 공통 구조(패턴)는 무엇인가요? 또 get_grade의 if-elif 구조에서 발견할 수 있는 규칙은 무엇인가요?",
      "focus_points": [
        "두 함수 모두 'scores를 for로 순회하며 값을 하나씩 누적/비교'하는 패턴을 공유",
        "get_grade는 높은 점수부터 차례로 경계값을 검사하는 규칙적 사다리 구조"
      ]
    },
    {
      "category": "abstraction",
      "category_ko": "추상화",
      "question": "get_grade 함수는 점수 구간과 등급의 관계를 내부에 숨기고 'A', 'B' 같은 결과만 돌려줍니다. 만약 이 함수가 없다면 print_report 안이 어떻게 복잡해질까요? 그리고 함수 이름만 보고도 기능을 짐작할 수 있는 이유는 무엇인가요?",
      "focus_points": [
        "함수가 없으면 print_report 안에 긴 if-elif가 그대로 들어가 읽기 어려워짐",
        "get_grade라는 이름이 '등급을 얻는다'는 의도를 드러내어 내부 구현을 몰라도 사용 가능(추상화)"
      ]
    },
    {
      "category": "algorithm",
      "category_ko": "알고리즘",
      "question": "get_grade 함수에 평균 점수 85가 들어왔다고 가정해 봅시다. 코드가 위에서부터 어떤 조건들을 차례로 검사하며, 최종적으로 어떤 값을 돌려주는지 순서대로 설명해 보세요.",
      "focus_points": [
        "90 이상? 아니오 → 80 이상? 예 → 'B' 반환, 이후 조건은 검사하지 않음",
        "조건문이 위에서 아래로 순차 검사되며 처음 참이 되는 가지에서 멈추는 흐름 이해"
      ]
    },
    {
      "category": "integration",
      "category_ko": "통합",
      "question": "print_report 함수 한 번이 실행될 때 앞의 세 함수가 어떤 순서로 함께 사용되나요? 그리고 맨 아래의 for 반복문은 이 부분들을 어떻게 하나로 합쳐, 모든 학생의 성적표를 출력한다는 전체 목적을 완성하나요?",
      "focus_points": [
        "print_report 내부에서 calculate_average → get_grade, find_highest를 호출해 결과를 조립",
        "맨 아래 for문이 students의 학생마다 print_report를 반복 호출해 전체 성적표를 완성"
      ]
    }
  ]
}
```

---

## 6. 조정 가능한 파라미터

생성할 때 아래 값만 바꿔 다양한 문제를 만든다.

| 파라미터 | 설명 | 예시 값 |
|---|---|---|
| `난이도` | 대상 학년. 코드의 문법 난이도와 어휘를 결정 | 중1 / 중3 / 고1 / 고2 |
| `주제` | 코드 소재(선택). 비우면 LLM이 자유 선택 | 성적 처리, 장바구니 합계, 단어 세기, 가위바위보, 온도 변환 |
| `코드_길이` | 대략적 줄 수 범위 | 30~50줄(기본) |
| `주석_포함` | 코드에 주석을 넣을지 | false(기본) / true |
| `문항_수` | 유형당 문항 개수 | 1(기본) / 2 |

**난이도별 권장 문법 범위(참고)**
- 중1~2: 변수, `if`, `for`, 리스트, `print`, 함수 1~2개
- 중3~고1: 함수 3~5개, 딕셔너리, `f-string`, 중첩 조건
- 고2 이상: 함수 분리 + 간단한 자료구조 조작, 누적/탐색 알고리즘

---

## 7. LLM 프롬프트 템플릿 (복사해서 사용)

> 1~5장의 규칙·스키마·예시는 **시스템 프롬프트**에 고정으로 넣고, 아래 사용자 메시지에서는 파라미터만 바꾸면 토큰을 아끼고 결과가 일관된다. Qwen3는 출력 형식 지시에 민감하므로 "JSON만 출력" 지시를 반드시 강조한다.

**[시스템 프롬프트에 넣을 내용]**

```
너는 중·고등학생의 '코드 읽기' 학습을 위한 문제 출제자다.
규칙:
1. 실행 가능한 단일 파이썬 프로그램 1개를 만든다(코드 조각 금지). 표준 라이브러리만, 외부 입력 없이 실행되도록 데이터는 코드 안에 고정한다.
2. 코드 길이는 약 30~50줄, 함수 3~5개로 기능을 나눈다. 변수·함수 이름은 의미가 드러나게 짓는다.
3. 코드에는 반드시 (a)구분되는 함수 2개 이상, (b)반복문, (c)함수로 세부를 감춘 부분, (d)조건 분기, (e)부분들이 합쳐져 전체 목적을 이루는 구조가 모두 들어가야 한다.
4. 그 코드를 읽고 답하는 문제 5개를 만든다: 분해·패턴인식·추상화·알고리즘·통합 각 1개. 문제는 코드를 읽으면 답할 수 있어야 하고, 구체적 함수명·줄을 가리키며, '설명/비교/추론' 형태로 묻는다.
5. focus_points에는 채점·안내용 핵심 포인트를 적되 학생에게 보일 정답 문장이 아니라 평가 기준으로 적는다.
6. 출력은 지정된 JSON 객체 1개만 한다. 코드블록 기호나 설명 문장을 절대 붙이지 않는다.

[여기에 4장의 JSON 스키마와 5장의 완성 예시를 붙여넣는다]
```

**[사용자 메시지: 파라미터만 교체]**

```
다음 조건으로 코드 읽기 문제 1세트를 JSON으로 생성하라.
- 난이도: 중3
- 주제: (자유 선택)
- 코드 길이: 30~50줄
- 주석 포함: false
- 유형당 문항 수: 1
```

---

## 8. 운영 체크리스트 (출제 후 1분 점검)

- [ ] 코드를 복사해 그대로 실행하면 오류 없이 돌아가는가
- [ ] 2.2의 다섯 구성요소가 코드에 모두 있는가
- [ ] 5개 문항이 각각 다른 사고력을 묻고 있는가(서로 겹치지 않는가)
- [ ] 모든 문항이 "코드를 읽으면" 답할 수 있는가
- [ ] 출력이 깨지지 않는 유효한 JSON인가 (Flask에서 파싱 성공)
- [ ] focus_points가 학생 화면에 노출되지 않도록 분리되어 있는가
