# 생성형 점진적 프롬프팅을 통한 컴퓨팅 사고력 향상 프로그램

캡스톤 프로젝트 | Flask 기반 Python 학습 보조 웹앱 | 제주도 중·고등학생 대상

---

## 프로그램 구조

Flask 웹앱. Python 학습 보조 도구.

### 전체 흐름
1. 사용자가 페이지를 열면 자동으로 주제별 Python 코드 예제 생성 (LM Studio 로컬 LLM 호출)
2. 해당 코드를 기반으로 문제 5개를 한 번에 하나씩 순차 생성 (난이도 매우쉬움→매우어려움)
3. 학생이 답변을 제출하면 다음 문제 생성
4. 5문제 완료 시 컴퓨팅 사고력 + 프롬프팅 품질 자동 평가

### LLM 연결
- LM Studio (localhost:1234/v1), OpenAI SDK 호환
- 채팅 모델: local-model (현재 Qwen3 8B)
- 임베딩 모델: text-embedding-bge-m3 (RAG용)

### RAG 구조
- 라이브러리: LangChain + FAISS
- rag_docs/code_examples/      ← 코드 예제 참고 TXT
- rag_docs/problem_templates/  ← 문제 출제 템플릿 TXT (난이도별 5개)
- rag_db/                      ← FAISS 인덱스 저장 (자동 생성)
- 청크 크기: 200자, 오버랩: 50자, TOP_K: 5

### 문제 생성 흐름 (api.py → rag.py)
POST /api/generate-problem 호출 시:
  - problem_index(0~4), previous_problems(중복방지), code 전달
  - rag_store.retrieve("problem_templates", f"{difficulty} {code[:300]}") 로 유사 템플릿 검색
  - LLM 프롬프트에 RAG 결과 + 난이도 + 중복금지 목록 삽입
  - 응답에서 첫 번째 비어있지 않은 줄을 문제로 추출

### 주요 파일
- app.py                       : Flask 앱 진입점
- api.py                       : 모든 API 엔드포인트 (Blueprint)
- rag.py                       : RAG 모듈 (FAISS 인덱싱 + 검색)
- static/main.js               : 프론트엔드 로직
- templates/index.html         : UI
- rag_docs/problem_templates/  : 문제 출제용 RAG 파일 위치

---

## 핵심 설계 원칙 — CT 5단계 문제 순서

문제 5개는 난이도 순서이면서 동시에 CT(컴퓨팅 사고력) 요소 순서여야 한다.
이 순서는 인지 부하 이론에 기반한 것으로 변경하지 않는다.

| problem_index | CT 요소       | 난이도     | 질문 방향                         |
|---------------|---------------|------------|----------------------------------|
| 0             | 분해          | 매우쉬움   | 코드를 역할별로 나눈다면?         |
| 1             | 패턴인식      | 쉬움       | 출력 패턴의 다음 값은?            |
| 2             | 추상화        | 보통       | 이 변수가 의미하는 것은?          |
| 3             | 알고리즘적사고| 어려움     | n번 반복 후 변수 값은?            |
| 4             | 통합          | 매우어려움 | 이 코드가 해결하는 문제는?        |

---

## CT 가이드 상세 내용 (TXT 파일 생성 시 사용)

### 분해 (problem_index=0, 매우쉬움)
질문 패턴:
- 이 코드를 역할에 따라 나눈다면 몇 단계인가?
- 이 함수가 담당하는 역할은 무엇인가?
- 코드에서 입력을 담당하는 부분은 어느 줄인가?

인지 조작: 전체 코드를 기능 단위로 분리하고 각 단위의 독립적 역할 파악
오답 설계: 역할이 비슷한 두 블록을 하나로 합치거나 단계 수를 1개 틀리게 제시
난이도 기준: 함수 이름이 직관적이고 블록 수가 2~3개인 코드 사용

예시 문제:
Q: 이 코드를 역할별로 나눈다면 가장 적절한 것은?
A. 입력 / 처리 / 출력  (정답)
B. 입력 / 출력
C. 정렬 / 출력
D. 입력 / 비교 / 교환 / 정렬 / 출력
해설: 코드는 데이터를 받고 처리한 뒤 결과를 출력하는 세 역할로 분해된다.

### 패턴인식 (problem_index=1, 쉬움)
질문 패턴:
- 이 반복문이 실행될 때 출력 패턴은?
- n번째 반복에서 출력되는 값은?
- 어떤 규칙으로 변수가 변하는가?

인지 조작: 반복 구조에서 변화 규칙을 추출하고 개별 결과가 아닌 일반 패턴 파악
오답 설계: n번째 대신 n±1번째 값 제시, 반복 횟수 1 틀리게 한 결과 제시
난이도 기준: 단순 등차/배수 패턴, 반복 3~5회 이내, 변수 1~2개

예시 문제:
Q: for i in range(1,6): print(i*2) 실행 시 세 번째로 출력되는 값은?
A. 4
B. 6  (정답)
C. 8
D. 10
해설: i=1→2, i=2→4, i=3→6 패턴을 파악하면 세 번째는 6이다.

### 추상화 (problem_index=2, 보통)
질문 패턴:
- 변수 {이름}이 나타내는 것은?
- 함수 {이름}의 역할을 가장 잘 설명한 것은?
- 왜 이 변수의 이름을 {이름}으로 지었을까?

인지 조작: 구체적 값이나 구현 방법이 아닌 변수·함수가 표현하는 개념·역할 파악
오답 설계: 자료형으로 오답 유도, 구체적 값을 역할로 혼동, 비슷한 이름 변수와 혼동
난이도 기준: 약어 또는 범용 이름(result, temp) 사용, 맥락 읽어야 파악 가능

예시 문제:
Q: 학생 점수 처리 코드에서 변수 total_score가 나타내는 것은?
A. 점수의 자료형
B. 모든 학생 점수의 합계  (정답)
C. 가장 높은 점수
D. 반복 횟수
해설: 변수명 total_score와 누적 연산 맥락에서 전체 합계임을 추론해야 한다.

### 알고리즘적사고 (problem_index=3, 어려움)
질문 패턴:
- n번 반복 후 변수 {이름}의 값은?
- 조건 {조건식}이 참일 때 실행되는 블록은?
- 이 코드를 실행한 최종 출력은?

인지 조작: 코드를 줄 단위로 순서대로 실행하며 변수 상태 추적
오답 설계: off-by-one(반복 1 틀리게), 조건 반대 결과, 중간 상태값을 최종값으로 제시
난이도 기준: 조건문 포함 반복문, 변수 3개, 반복 5회 이내

예시 문제:
Q: x=0 / for i in range(1,4): x=x+i 실행 후 x의 값은?
A. 3
B. 6  (정답)
C. 4
D. 9
해설: i=1→x=1, i=2→x=3, i=3→x=6으로 순서대로 추적해야 한다.

### 통합 (problem_index=4, 매우어려움)
질문 패턴:
- 이 코드가 해결하는 문제는?
- 이 코드의 최종 목적으로 가장 적절한 것은?
- 어떤 상황에서 이 코드를 사용하면 유용한가?

인지 조작: 분해·패턴·추상화·알고리즘 전체를 종합하여 코드 전체 목적 도출
오답 설계: 중간 과정을 전체 목적으로 혼동, 비슷하지만 목적 다른 알고리즘 제시
난이도 기준: 함수명·변수명에 목적이 드러나지 않고 전체 흐름에서만 파악 가능

예시 문제:
Q: 리스트를 처리하는 코드(함수명 없음)의 목적으로 가장 적절한 것은?
A. 리스트를 오름차순으로 정렬한다
B. 리스트에서 가장 큰 값을 찾는다  (정답)
C. 리스트의 평균을 계산한다
D. 리스트에서 중복 값을 제거한다
해설: 비교·갱신 패턴과 최종 반환값을 종합하면 최댓값 탐색임을 알 수 있다.

---

## 현재 작업 목록 (Claude Code가 이 순서대로 구현)

### Task 1 — CT 가이드 TXT 파일 5개 생성
위치: rag_docs/problem_templates/
파일명: ct_분해.txt, ct_패턴인식.txt, ct_추상화.txt, ct_알고리즘적사고.txt, ct_통합.txt

각 파일에 포함할 내용 (위 CT 가이드 상세 내용 참고):
- CT 요소명과 problem_index, 난이도 명시
- 질문 패턴 목록
- 인지 조작 설명
- 오답 설계 원칙
- 난이도 기준
- 예시 문제 2개 (보기 4개, 정답, 해설 포함)

### Task 2 — api.py 수정
아래 변경사항을 기존 코드 스타일에 맞게 적용한다.

2-1. 파일 상단에 CT 매핑 상수 추가:
```python
CT_SKILL_MAP = {
    0: "분해",
    1: "패턴인식",
    2: "추상화",
    3: "알고리즘적사고",
    4: "통합",
}
```

2-2. /api/generate-problem 엔드포인트 내부 수정:
- ct_skill = CT_SKILL_MAP[problem_index] 추출
- RAG 쿼리 변경: f"{difficulty} {code[:300]}" → f"{ct_skill} {difficulty} {code[:300]}"
- LLM 프롬프트에 CT 요소 추가: "[CT 요소: {ct_skill}]" 섹션 삽입
- 응답 JSON에 ct_skill 필드 추가: {"problem": ..., "ct_skill": ct_skill, ...}

2-3. 프롬프트 구성 예시 (기존 프롬프트 구조 유지하되 CT 요소 삽입):
```
다음 파이썬 코드를 읽고 {ct_skill} 능력을 측정하는 객관식 문제 1개를 만들어라.

[CT 요소: {ct_skill}]
[난이도: {difficulty}]
[참고 템플릿]
{rag_templates}

[파이썬 코드]
{code}

[이미 출제된 문제 - 중복 금지]
{previous_problems}

규칙:
- 보기 4개 (A/B/C/D)
- 정답과 해설 포함
- {ct_skill}을 측정하는 질문 방향으로 출제
- 첫 줄에 문제만 출력
```

### Task 3 — FAISS 인덱스 재빌드
TXT 파일이 추가되었으므로 기존 인덱스를 삭제하고 재빌드한다.
- rag_db/ 디렉토리 삭제
- app.py 실행 시 자동으로 재빌드되는지 확인
- 재빌드가 자동이 아니라면 수동 재빌드 코드 실행

### Task 4 — 동작 확인
- Flask 서버 실행 후 /api/generate-problem 에 테스트 요청 전송
- problem_index 0~4 각각에 대해 ct_skill이 올바르게 반환되는지 확인
- RAG 검색 결과에 CT 가이드 TXT 내용이 포함되는지 확인

---

## 주의사항

- CT 5단계 순서(분해→패턴인식→추상화→알고리즘→통합)는 변경하지 않는다
- 소크라테스 챗봇이 있다면 정답을 직접 제공하지 않도록 유지한다
- 기존 rag.py 검색 로직은 수정하지 않는다 (쿼리 문자열만 변경)
- 기존 LLM 호출 방식(OpenAI SDK 호환)은 유지한다
