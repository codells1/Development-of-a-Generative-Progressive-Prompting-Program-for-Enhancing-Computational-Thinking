# code_examples (통합 주제) — 교체용 예제 코드

기존 `code_examples`의 코드가 너무 단순해서, **통합(융합) 주제에 맞는 완결형 예제 코드**로 교체하기 위한 파일들입니다.

## 들어 있는 것

| 파일 | 주제 | 난이도 | 결합 개념 |
|---|---|---|---|
| `통합_01_도서대출.txt` | 도서 대출 관리 | 중3~고1 | 딕셔너리·리스트·함수·조건·반복 |
| `통합_02_편의점영수증.txt` | 영수증·거스름돈 | 중3~고1 | 리스트·딕셔너리·함수·반복·산술 |
| `통합_03_단어빈도.txt` | 단어 빈도 분석 | 고1~고2 | 문자열·딕셔너리·정렬·함수·반복 |
| `통합_04_퀴즈채점.txt` | 객관식 자동 채점 | 중3~고1 | 리스트·딕셔너리·함수·조건·반복 |
| `통합_05_가계부요약.txt` | 월별 가계부 요약 | 고1~고2 | 리스트·딕셔너리·누적집계·함수·조건 |

5개 모두 **외부 입력 없이 그대로 실행되는 완결형 프로그램**이고, 실행 검증을 마쳤습니다. 각 프로그램은 여러 함수가 모여 하나의 목적을 이루는 구조라, 통합 문항뿐 아니라 분해·패턴인식·추상화·알고리즘 문항도 함께 만들 수 있습니다.

## 두 가지 참고

- **저작권**: 실제 교과서 코드를 그대로 옮긴 것이 아니라, 2022 개정 교육과정 정보 교과 범위에 맞춰 **교과서 스타일로 새로 작성한 원본**입니다. RAG DB에 넣어도 라이선스 문제가 없습니다.
- **상단 주석**: 각 파일 맨 위 `# 주제/난이도/개념/통합 포인트` 주석은 **RAG 검색·생성 시 참고용 메타데이터**입니다. 학생에게 보여줄 코드가 아니며, 생성기는 이를 참고해 새 코드를 만들되 학생용 코드는 `prompts/code_reading_generation.md` 기준대로 주석을 최소화해 출력하면 됩니다.

## 적용 방법 (ChromaDB 재인덱싱)

1. 기존 `code_examples` 폴더의 **단순했던 예제 파일들을 제거**하고, 이 5개 `.txt`를 넣습니다.
2. 인덱스를 재빌드합니다(기존 워크플로 그대로): `rag_db/` 폴더 삭제 후 앱 재시작 → 자동 재임베딩.

### ⚠️ 청크 크기 주의

이 예제들은 한 개당 약 40~50줄(≈1,500자 이상)이라, 기존 `code_examples` 청크 크기(400~500자)로 그대로 임베딩하면 **한 프로그램이 여러 조각으로 잘려** "코드를 통째로 검색"하려던 목적이 깨집니다. 권장:

- `code_examples`는 **파일 1개 = 문서(ID) 1개**로 통째 임베딩 (문자 단위 분할 끄기), 또는
- 부득이 분할한다면 청크 크기를 **2,000자 이상**으로 키워 한 프로그램이 한 청크에 들어가게.

(`problem_templates` 컬렉션 설정은 건드리지 마세요. 거긴 그대로 200자 유지.)

## 더 필요하면

지금은 **통합 주제만** 채웠습니다. 원하시면 (1) 통합 예제를 더 추가하거나, (2) 다른 주제 축(순차·조건 / 반복·리스트 / 함수 / 알고리즘)도 같은 형식으로 만들어 드리고, (3) 난이도(중1~고2)를 더 세분화해 드리겠습니다.
