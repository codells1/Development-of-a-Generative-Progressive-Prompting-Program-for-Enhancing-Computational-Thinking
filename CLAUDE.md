# 컴퓨팅 사고력 측정을 위한 생성형 점진적 프롬프팅 프로그램 개발

캡스톤 프로젝트 | Flask 기반 Python 코드 읽기 학습·CT 측정 웹앱 | 제주도 중·고등학생 대상

---

## 프로젝트 목표

학생이 소크라테스식 LLM 챗봇과 대화하며 **하나의 완결된 파이썬 코드를 읽고 이해**하는 과정에서, 그 **대화 로그를 분석해 컴퓨팅 사고력(CT)을 측정**하는 웹 플랫폼.

---

## 기술 스택

- **Backend**: Flask 3.x (Python 3.x)
- **LLM**: **Qwen3 8B** — LM Studio 로컬 실행(OpenAI 호환 API, Thinking 모드 가능). 코드·문제 생성, 소크라테스 챗봇, 세션 후 분석에 공통 사용. 외부 클라우드 API는 쓰지 않음.
- **임베딩**: **bge-m3**
- **벡터 스토어 / RAG**: **ChromaDB** — **문제 출제·코드 생성에만** 사용 (CT 가이드·문제 템플릿 문서 참조). 평가에는 사용하지 않음(아래 주의사항 참고).
- **DB**: SQLite3 (세션·대화 로그·문제풀이 기록·피드백 저장)
- **Frontend**: HTML / CSS / JavaScript + Fetch API(비동기)
- **개발 환경**: VS Code, GitHub / 로컬 RTX 4060 8GB (Qwen3 8B는 Q4_K_M 기준으로 VRAM 한도 안에서 실행)

---

## 화면 구성 (3분할)

- **좌측**: 파이썬 코드 1개 (완결된 실행 가능 프로그램)
- **우상단**: 이해 문제 5개 (CT 5유형)
- **우하단**: 소크라테스 챗봇 (정답 금지, 유도 질문만)

---

## 전체 흐름

1. 페이지 진입 → **완결된 파이썬 코드 1개**를 Qwen3 8B로 생성·제시
   - 단계별 코드 조각 ❌ → 위에서 아래로 실행되는 하나의 프로그램 ✅
   - 생성 시 **RAG**(ChromaDB)로 CT 가이드·문제 템플릿 문서를 참조
2. 그 코드에 대한 이해 문제 **5개를 한 세트로 생성** — 분해 · 패턴인식 · 추상화 · 알고리즘 · 통합 각 1문항
   - 코드·문제 생성 사양: `prompts/code_reading_generation.md` (필요 시 열어볼 것)
   - 난이도는 **세트 단위 파라미터**(예: 중3). 구버전의 "문항별 매우쉬움→매우어려움 단계 상승"은 폐기.
3. 학생이 문제를 풀며 우하단 소크라테스 챗봇과 대화 (유도 질문만)
4. 세션 종료 → 완결된 대화 로그를 **별도 세션의 Qwen3 8B**로 분석해 **CT 발현 피드백** 제공

---

## 문제 5유형 (CT 매핑 · 순서 고정)

| 순서 | CT 요소 | 코드 읽기 초점 |
|---|---|---|
| 1 | 분해 | 코드를 함수·블록 단위로 나누기 |
| 2 | 패턴인식 | 반복 구조 · 유사 함수 · 규칙적 분기 |
| 3 | 추상화 | 함수/변수가 감추는 것, 대표하는 것 |
| 4 | 알고리즘 | 실행 순서 · 조건 분기 흐름 추적 |
| 5 | 통합 | 부분들이 합쳐져 이루는 전체 목적 |

- 각 문항에는 `focus_points`(채점·챗봇 유도용 핵심 포인트)를 함께 생성한다.
- ⚠️ `focus_points`는 **학생 화면에 절대 노출 금지**. 챗봇 내부 기준 + 세션 후 CT 채점 근거로만 사용.

---

## 소크라테스 챗봇 규칙 (절대 원칙)

- **절대 금지**: 정답, 직접 힌트, 코드 설명, "맞아요/틀렸어요" 식 직접 평가
- **허용**: 유도 질문 ("그렇게 생각한 이유는?", "이 부분 다시 읽어볼까?")
- 질문은 **1~2개로 제한**
- 시스템 프롬프트에 해당 문항의 `focus_points` / `hint_keywords`를 주입해 질문 방향만 유도
- 대화 로그는 세션 종료 후 분석에 사용

---

## 세션 후 분석 (피드백)

- 완결된 대화 로그를 분석해 **CT 7지표**를 평가한다:
  **문제분해 · 용어사용 · 추상화 · 실행흐름 · 자료표현 · 동작파악 · 자기해결**
- 각 지표 = 척도 점수 + 근거(대화 로그 인용) + 개선 방향. 출력은 JSON.
- **객관성 확보**: 분석은 튜터링 챗봇과 **분리된 별도 세션(새 컨텍스트)**의 Qwen3 8B로 수행한다. 챗봇 대화 맥락을 이어받지 않고, 완결된 로그만 입력으로 받아 평가한다.
- **평가에는 RAG를 쓰지 않는다.** 루브릭은 분석 프롬프트에 직접 포함한다 (RAG 사용 시 객관성·정확성 저하 우려).
- 프롬프팅 품질 평가는 **현재 범위에서 제외(잠정 보류)** — 관련 기능을 새로 추가하지 말 것.

---

## Flask API 엔드포인트 (현재 설계)

- `POST /api/session/start` → 코드 1개 + 5문항 세트 생성(RAG 사용), `session_id` 발급
- `POST /api/chat` → 소크라테스 응답 생성, 대화 로그 저장
- `POST /api/session/analyze` → 완결된 로그를 별도 세션 Qwen3 8B로 분석 → CT 7지표 피드백(RAG 미사용)

---

## 개발 시 주의사항

1. **단일 완결 코드 원칙**: 좌측 코드는 항상 실행 가능한 하나의 프로그램. 조각으로 쪼개지 말 것. (구성요소·길이 기준은 `prompts/code_reading_generation.md`)
2. **소크라테스 원칙 준수**: 챗봇이 정답을 흘리는 방향으로 코드가 바뀌면 안 됨.
3. **CT 5유형 순서 고정**: 분해→통합 순서는 핵심 설계. 임의 셔플 금지.
4. **focus_points 비노출**: 학생에게 보내는 응답 JSON에 `focus_points`를 절대 포함하지 말 것.
5. **로컬 LLM 단일화**: 생성·챗봇·분석 모두 LM Studio의 Qwen3 8B로 처리. 외부 클라우드 API 호출을 새로 추가하지 말 것.
6. **RAG 사용 범위 제한**: RAG(ChromaDB)는 **문제 출제·코드 생성에만** 사용. **평가(세션 후 분석)에는 절대 사용하지 말 것** — 객관성·정확성 저하 우려.
7. **분석 세션 분리**: 세션 후 분석은 튜터링 대화와 분리된 새 세션에서 실행. 챗봇 컨텍스트를 재사용하지 말 것.
8. **프롬프팅 평가 보류**: 프롬프팅 품질 평가 기능은 현재 제외 상태. 임의로 재도입하지 말 것.
9. **설계 기준 문서 동기화**: `prompts/code_reading_generation.md`나 `rag_docs/`의 문서를 수정하면 본 CLAUDE.md도 함께 갱신.

---

## 현재 개발 상태

- [x] CT 평가 루브릭 / CT 가이드 문서
- [x] 코드읽기 문제생성 사양서 (`prompts/code_reading_generation.md`)
- [ ] 단일 완결 코드 + 5문항 세트 생성 (Qwen3 8B + RAG)
- [ ] ChromaDB RAG(CT 가이드·문제 템플릿 참조) 연동 — 생성 전용
- [ ] Flask route 연결
- [ ] 소크라테스 챗봇 구현
- [ ] 세션 종료 후 CT 7지표 분석 모듈 (별도 세션, RAG 미사용)
