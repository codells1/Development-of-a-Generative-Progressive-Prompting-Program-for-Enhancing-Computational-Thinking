# 출제 모듈 확장 스펙 — 문제 유형 다양화

> 이 문서는 Claude Code 작업 지시서이자 RAG 출제 변경의 기준 문서다.
> `<...>` 부분은 실제 코드베이스에 맞게 채운다.

## 1. 목표
- 현재: 코드 생성(완료) → RAG 기반 출제(완료). 단, 출제는 "코드 자체" 문제만 만든다.
- 변경: 출제 모듈 + RAG 검색 조건화를 확장해 **두 트랙**을 운영한다.
  - **코드 기반 트랙**: 같은 생성 코드로 실행 순서·변수 추적·출력 예측·순서도를 출제(기존 "코드 자체" 유형 유지).
  - **비코드 트랙**: 분해·패턴인식을 코드 없이 '문제상황' 문항으로 출제.

## 2. 범위 (Scope) — 매우 중요
- 변경 O:
  - 출제 모듈 (`<예: src/quiz/generator.py>`)
  - 출제 프롬프트 (`<예: prompts/question_gen.md>`)
  - RAG 검색 코퍼스 및 검색 조건화 (7절) — 인프라(벡터스토어/임베딩)는 유지, 코퍼스·필터만 조정
  - 비코드 트랙용으로 `ct_분해.txt` / `ct_패턴인식.txt`를 비코드 버전으로 교체(별도 제공됨)
- 변경 X:
  - 코드 생성 모듈 (`<경로>`)
  - RAG 검색 엔진/인덱싱 파이프라인 구조
- 한 번에 모든 유형을 만들지 말 것. 1유형씩 추가 → 검증 → 다음.

## 3. 핵심 설계 원칙 (반드시 지킬 것)
1. **역할 분리**
   - 코드 기반 유형: RAG/LLM은 *지문·보기·오답·해설*만 생성하고, **정답은 코드로 계산**한다.
   - 비코드 유형: 코드가 없으므로 LLM이 문항 전체(정답 포함)를 생성하되, **정답은 검증 가능하거나 저작 단계에서 확정**되어야 한다(아래 2항·4절).
2. **검증은 유형별로 다르다.** 모든 문제는 채택 전 아래 방식 중 하나로 정답이 보장돼야 한다.

| 트랙 | 유형 | 정답 검증 방식 |
|---|---|---|
| 코드 | 실행 순서 · 변수 추적 · 출력 예측 · 코드 자체 | 실행 트레이스로 산출·대조 |
| 코드 | 순서도 | AST에서 결정적 변환·대조 |
| 비코드 | 패턴인식 | **규칙으로 해당 항을 계산해 자동 검증** (코드 실행 아님) |
| 비코드 | 분해 | **자동 검증 불가** → intent-first 저작(정답 먼저 확정) + 사람 검수 |

   - 공통: 어떤 방식이든 각 오답이 정답과 같은 결과가 아님을 확인한다.
3. 유형 무관 공통 출력 스키마 유지(5절).

## 4. 유형 정의

### 4.A 코드 기반 유형

#### 실행 순서 고르기  ← 코드 트랙 우선순위 1
- 정답 산출: 트레이스로 실행된 줄 번호 시퀀스 캡처 = 정답
- 오답: 줄 순서 일부 swap, 반복 횟수 ±1

#### 변수 추적표
- 정답 산출: 트레이스에서 각 스텝의 변수 스냅샷 / 출제: 표 일부 빈칸

#### 출력 예측
- 정답 산출: 실행 결과(stdout) 캡처 / 오답: off-by-one·연산자 변이 출력

#### 순서도
- 정답 산출: AST → 노드/엣지 그래프 변환 후 렌더(mermaid/graphviz)
- 출제: 선다(올바른 순서도) 또는 빈 블록 채우기 / 오답: 분기·반복 조건 반전

### 4.B 비코드 유형

#### 분해 (Decomposition)
- 입력: 실생활 문제상황(글/그림). **코드 없음.**
- 출제: 작업을 단계·하위작업으로 분할 / 묶기 / 단계 수.
- 정답 산출: **intent-first** — 올바른 분할을 먼저 정하고 그에 맞는 상황을 생성. 자동 검증 불가 → 사람 검수.
- 오답: 두 단계 합치기, 단계 수 ±1, 한 작업을 둘로 쪼개기, 목적을 한 단계로 끼워넣기.

#### 패턴인식 (Pattern Recognition)
- 입력: 도형·기호·숫자·사건의 반복 나열. **코드 없음.**
- 출제: 다음 항 / n번째 항 / 규칙 / 주기.
- 정답 산출: 규칙(공차·공비·순환 주기)으로 해당 항을 **결정적으로 계산** → 자동 검증 가능.
- 오답: off-by-one, 시작값 변경, 등차↔등비 혼동, 주기 한 칸 어긋남.

## 5. 출력 스키마 (기존 호환 + 검증 필드)
```json
{
  "type": "code_self | execution_order | variable_trace | output_predict | flowchart | decomposition | pattern",
  "track": "code | noncode",
  "stem": "지문(한국어)",
  "choices": ["보기1", "보기2", "보기3", "보기4"],
  "answer": "정답",
  "explanation": "해설(한국어)",
  "source_code": "코드 기반 유형일 때만",
  "verify_method": "trace | ast | rule | authored",
  "verified": true
}
```
- 기존 필드는 그대로 두고 `track` / `verify_method`만 추가한다.

## 6. RAG 출제 프롬프트에 추가/교체할 지시문 (예시)
```
주어진 입력으로부터 지정된 유형의 문제를 출제한다.

[공통 규칙]
- 지문, 보기(선다 4개), 해설을 한국어로 작성한다.
- 오답 보기는 무작위가 아니라 흔한 오개념을 반영한다(off-by-one, 단계 합치기 등).

[코드 기반 유형]
- ★ 정답을 직접 계산하지 말 것. 보기 후보만 작성하고, 정답은 시스템이 코드를 실행/분석해 확정한다.
- 예) 실행 순서 고르기 / 출력 예측 …

[비코드 유형]
- ★ 패턴인식: 규칙(공차·공비·주기)을 정하고 그 규칙으로 정답 항을 계산해 명시한다(검증 가능해야 함).
- ★ 분해: 올바른 단계 분할(정답)을 먼저 정하고, 그 분할에 맞는 실생활 상황을 작성한다.
```

## 7. RAG 검색(retrieval) 조정
- 새 유형마다 형식·정답이 검증된 gold 예제 2~3개를 인덱싱하고 `type` 메타데이터 부착.
- 출제 대상 유형으로 메타데이터 필터(또는 쿼리에 유형 포함)해 같은 유형 예제만 retrieve.
- 비코드 트랙: `ct_분해.txt` / `ct_패턴인식.txt` 비코드 버전을 코퍼스에 반영.
- 예제 데이터 위생: LLM이 예제를 모방하므로 정답·형식이 정확해야 한다.

## 8. 제약
- 코드 실행은 격리 환경 + 타임아웃(예: 2초) 필수. 비코드 유형은 코드 실행 없음.
- 외부 네트워크/파일 접근 금지. 한국어 톤 유지.

## 9. 완료 기준 (Acceptance Criteria)
- [ ] 코드 트랙: 우선순위 1유형 동작 + 실행 검증 게이트 + 단위 테스트
- [ ] 패턴인식(비코드): 규칙 계산 검증 헬퍼로 정답·오답 자동 확인
- [ ] 분해(비코드): intent-first 저작 + 사람 검수 절차 마련(자동 검증은 미요구)
- [ ] 기존 "코드 자체" 유형 회귀 없음
- [ ] diff가 출제 모듈/프롬프트/코퍼스에 국한(검색 엔진·코드생성 변경 0)

## 10. 작업 순서 (Claude Code용)
1. 코드베이스 탐색: 출제 모듈 구조·출력 스키마·RAG 호출/검색 지점·코퍼스 위치 요약.
2. 변경 계획 제안(트랙별). **승인 전 코드 수정 금지.**
3. 트랙별 구현:
   - 코드 기반: 우선순위 1유형 + 실행 검증 게이트 + 테스트.
   - 비코드: 패턴인식 규칙-검증 헬퍼 / 분해 intent-first 저작·검수 흐름.
4. 테스트·검증 결과 보고 후 다음 유형.
